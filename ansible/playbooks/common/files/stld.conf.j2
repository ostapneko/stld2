start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

respawn
respawn limit 10 5

setuid {{ stld_user }}
setgid {{ stld_user }}

exec /home/{{ stld_user }}/apps/stld/current/bin/stld
